# The brief's recommended project paths, made real rather than left to 404.
# They resolve to the homepage sections instead of thin duplicate pages, so
# there is one canonical copy of every claim.
/gocommerce       /#gocommerce       302
/svelte-commerce  /#svelte-commerce  302
/growth           /#growth           302
/architecture     /#architecture     302
/quick-start      /#quickstart       302
/docs             https://github.com/itswadesh/gocommerce#readme  302
/roadmap          /#growth           302

# Do not re-add a `/* /404 404` splat here. It was tried and does nothing on
# this project: an unmatched path returns 404 with a zero-length body either
# way. 404.html is deployed and /404 serves it correctly, the named rules above
# work, and it is not edge cache — so the cause is a Pages project setting, not
# this file. Check the build output directory and whether a Worker route is
# bound to the custom domain ahead of Pages.
