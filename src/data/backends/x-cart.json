{
  "slug": "x-cart",
  "name": "X-Cart",
  "title": "X-Cart SvelteKit Storefront — Svelte Commerce Connector",
  "description": "Svelte Commerce on X-Cart 5.5.1+: 26 of 43 services wired, with cart, checkout and sign-in; vendors are the gap. Setup, API key, environment and fixes.",
  "lede": "Solid core coverage: cart, checkout and sign-in are wired against X-Cart’s modern API, and the one recorded gap is vendors. Every call carries the store’s API key; a shopper’s token joins it after sign-in.",
  "verdict": "Cart, checkout and sign-in are wired; vendors are the one recorded gap. Cart and checkout need X-Cart’s read/write API key, and the connector takes it from a `PUBLIC_` variable, which the browser receives.",
  "quickStart": [
    {
      "text": "get the storefront",
      "cmd": "git clone https://github.com/itswadesh/svelte-commerce\ncd svelte-commerce"
    },
    {
      "text": "one connector at a time: remove the two the storefront ships with, add this one",
      "cmd": "npm uninstall @misiki/litekart-connector @misiki/vendure-connector\nnpm i @misiki/x-cart-connector"
    },
    {
      "text": "point it at your X-Cart store",
      "cmd": "echo PUBLIC_X_CART_API_URL=https://your-store.example.com >> .env\necho PUBLIC_X_CART_API_KEY=... >> .env"
    },
    {
      "text": "run it",
      "cmd": "npm run dev"
    }
  ],
  "quickStartNote": "The guide removes only `@misiki/litekart-connector`. A fresh clone’s `package.json` also lists `@misiki/vendure-connector`, and with two connectors installed and neither of them Litekart’s, the build stops and asks for `PUBLIC_CONNECTOR` — so this removes both.",
  "env": [
    {
      "name": "PUBLIC_X_CART_API_URL",
      "required": true,
      "note": "Your X-Cart store’s URL. Boot fails, naming this variable, if it is missing."
    },
    {
      "name": "PUBLIC_X_CART_API_KEY",
      "required": false,
      "note": "The store API key from X-Cart’s Settings › API, sent as `X-Auth-Token` on every call. A read-only key rejects writes, so cart and checkout need the read/write key. See the limitations."
    },
    {
      "name": "PUBLIC_X_CART_ACCESS_TOKEN",
      "required": false,
      "note": "Sent as a bearer token when set; a shopper’s token after sign-in travels the same way."
    },
    {
      "name": "PUBLIC_CONNECTOR",
      "required": false,
      "note": "Only when more than one connector is installed: `@misiki/x-cart-connector` names the one this build runs on."
    }
  ],
  "envNote": "`init.ts` hands the connector every `PUBLIC_X_CART_*` variable that is set, camel-cased. The guide also lists `PUBLIC_X_CART_API_SECRET`, `PUBLIC_X_CART_ACCESS_KEY`, `PUBLIC_X_CART_STORE_ID` and `PUBLIC_X_CART_CHANNEL_ID`; the connector’s source does not read them.",
  "backendSettings": [
    "Run X-Cart 5.5.1 or later. The connector follows the modern API; the `_key` query parameter of the 5.3/5.4 admin API does not exist there and is not used.",
    "Create the API key under Settings › API — the read/write one, because a read-only key rejects every cart and checkout write.",
    "Let the storefront’s origin make cross-origin requests to X-Cart (CORS): in production the browser calls `PUBLIC_X_CART_API_URL` directly, not only the server."
  ],
  "howItWorks": [
    "X-Cart 5.5.1 and later want two headers: `X-Auth-Token`, the store API key, on every call, and `Authorization: Bearer`, the shopper’s token, once they have signed in. A read-only key is enough to browse but rejects every POST, PATCH and DELETE.",
    "Installing the package is the whole switch. `vite.config.ts` resolves whichever `@misiki/*-connector` is installed, and `src/lib/core/connectors/init.ts` hands it every `PUBLIC_X_CART_*` variable that is set, camel-cased, at boot — in the server hook for server rendering and in the client hook for the browser, which must reach the same URL in production.",
    "There is no Litekart API behind the storefront here, so store identity — name, logo, currency, menus, plugin toggles, theme colours — comes from `src/lib/core/connectors/default-store.json` merged under the default export of `kitcommerce.config.ts`. Until you override it, the store is called “Test”. Any Litekart REST path the connector still inherits is answered from that local data, or resolved empty, rather than requested."
  ],
  "limitations": [
    "Vendors: not wired.",
    "Pages that depend on a service that is not wired render their empty state rather than failing.",
    "The store API key, sent as `X-Auth-Token`, comes from `PUBLIC_X_CART_API_KEY` — and cart and checkout need the read/write key. SvelteKit sends `PUBLIC_` variables to the browser, so every visitor to the store can read it.",
    "Homepage sections other than the live product list come from the active theme’s static content — the standing rule for themes.",
    "The Conversational Shopping assistant is Litekart-only; its widget stays hidden when another connector is active."
  ],
  "troubleshooting": [
    {
      "problem": "Boot fails with “the x-cart connector is active but PUBLIC_X_CART_API_URL is not set”",
      "fix": "Add `PUBLIC_X_CART_API_URL` to `.env`. A production Node build and Docker read `PUBLIC_*` from the process environment rather than `.env`, so set it on your deploy platform too."
    },
    {
      "problem": "The build stops with “Several commerce connectors are installed … Set PUBLIC_CONNECTOR to pick one”",
      "fix": "More than one `@misiki/*-connector` is in `package.json` and none is Litekart’s. Uninstall the ones you do not run — a fresh clone also lists `@misiki/vendure-connector` — or set `PUBLIC_CONNECTOR='@misiki/x-cart-connector'`."
    },
    {
      "problem": "Boot fails with “PUBLIC_…_API_URL is set, but it configures a different backend than the one this build runs on”",
      "fix": "Another backend’s variable is still set, usually left over from the one you switched from. Remove it, or install that backend’s connector. `PUBLIC_LITEKART_API_URL` is exempt: it only points the dev proxy."
    },
    {
      "problem": "The store is called “Test”, or shows the wrong name and logo",
      "fix": "Store identity is static on this backend. Set `name`, `logo`, `currencyCode` and the rest in the default export of `kitcommerce.config.ts`; `src/lib/core/connectors/default-store.json` lists every field."
    },
    {
      "problem": "`[x-cart] no native implementation for get /api/...` in the console",
      "fix": "The connector’s REST guard caught a path inherited from Litekart and answered it empty instead of requesting it. Each path is reported once. Harmless if X-Cart has no such feature; if it has one, the fix belongs in the connector’s matching service."
    },
    {
      "problem": "`http proxy error: api/... ECONNREFUSED` in dev",
      "fix": "A Litekart REST path was requested from outside the connector, so its guard never saw it. Vite proxies `/api` to `PUBLIC_LITEKART_API_URL`, or to `localhost:7000`."
    },
    {
      "problem": "The build fails on `@misiki/litekart-connector`",
      "fix": "`@misiki/kitcommerce-core` declares it as a peer, and `vite.config.ts` redirects that specifier to whichever connector is installed. Make sure exactly one `@misiki/*-connector` is listed in `package.json`."
    }
  ],
  "faq": [
    {
      "q": "Can I run an X-Cart store on Svelte Commerce today?",
      "a": "Cart, checkout and sign-in are wired, and vendors are the only recorded gap. The obstacle is the key: cart and checkout need X-Cart’s read/write API key, which the connector reads from a `PUBLIC_` variable that every visitor’s browser receives."
    },
    {
      "q": "How do I switch Svelte Commerce to X-Cart?",
      "a": "Remove the connectors the storefront ships with, install `@misiki/x-cart-connector`, set `PUBLIC_X_CART_API_URL` in `.env` and start the dev server. `vite.config.ts` resolves whichever connector is installed, so no file in the storefront changes."
    },
    {
      "q": "Which X-Cart versions and keys does the connector need?",
      "a": "X-Cart 5.5.1 or later, and the read/write store API key from Settings › API, sent as `X-Auth-Token`. A read-only key can browse but rejects cart and checkout writes."
    },
    {
      "q": "Has the X-Cart connector been run against a real X-Cart store?",
      "a": "Not against a live production instance — no Svelte Commerce connector has been. It was written by reading X-Cart’s authoritative API source, and the Svelte Commerce maintainers ask X-Cart’s own maintainers for a sandbox to run it against."
    }
  ],
  "docUrl": "https://github.com/itswadesh/svelte-commerce/blob/main/docs/X-CART.md",
  "read": "2026-09-25"
}
