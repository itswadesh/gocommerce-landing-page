{
  "slug": "cs-cart",
  "name": "CS-Cart",
  "title": "CS-Cart SvelteKit Storefront — Svelte Commerce Connector",
  "description": "Svelte Commerce on CS-Cart: 20 of 43 services wired. Browsing and cart work, but there is no shopper sign-in and the storefront cannot complete an order.",
  "lede": "The CS-Cart connector covers browsing and the cart through CS-Cart’s REST API, which has no checkout resource and no shopper sign-in. It authenticates every call with an admin’s e-mail and API key.",
  "verdict": "Shoppers can browse and fill a cart, but cannot sign in, and the storefront cannot complete an order. CS-Cart’s REST API has no checkout resource; the one call that creates an order sits in the connector’s `orderService.codCheckout()`, which the storefront’s payment step does not call, so every way it places an order throws `NotSupportedError`.",
  "quickStart": [
    {
      "text": "get the storefront",
      "cmd": "git clone https://github.com/itswadesh/svelte-commerce\ncd svelte-commerce"
    },
    {
      "text": "one connector at a time: remove the two the storefront ships with, add this one",
      "cmd": "npm uninstall @misiki/litekart-connector @misiki/vendure-connector\nnpm i @misiki/cs-cart-connector"
    },
    {
      "text": "point it at your CS-Cart store",
      "cmd": "echo PUBLIC_CS_CART_API_URL=https://your-store.example.com >> .env\necho PUBLIC_CS_CART_API_KEY=admin@example.com >> .env\necho PUBLIC_CS_CART_API_SECRET=... >> .env"
    },
    {
      "text": "run it",
      "cmd": "npm run dev"
    }
  ],
  "quickStartNote": "The guide removes only `@misiki/litekart-connector`. A fresh clone’s `package.json` also lists `@misiki/vendure-connector`, and with two connectors installed and neither of them Litekart’s, the build stops and asks for `PUBLIC_CONNECTOR` — so this removes both.",
  "env": [
    {
      "name": "PUBLIC_CS_CART_API_URL",
      "required": true,
      "note": "Your CS-Cart store’s URL. Boot fails, naming this variable, if it is missing."
    },
    {
      "name": "PUBLIC_CS_CART_API_KEY",
      "required": false,
      "note": "Despite the name, an admin’s e-mail address — the HTTP Basic username. See the limitations."
    },
    {
      "name": "PUBLIC_CS_CART_API_SECRET",
      "required": false,
      "note": "The API key generated for that admin in the CS-Cart admin panel — the HTTP Basic password."
    },
    {
      "name": "PUBLIC_CS_CART_STORE_ID",
      "required": false,
      "note": "Company ID for company-scoped routes on CS-Cart Ultimate and Multi-Vendor, such as `/api/stores/{id}/orders`. `PUBLIC_CS_CART_CHANNEL_ID` is read if it is unset."
    },
    {
      "name": "PUBLIC_CS_CART_CHANNEL_ID",
      "required": false,
      "note": "Read as the company ID when `PUBLIC_CS_CART_STORE_ID` is unset."
    },
    {
      "name": "PUBLIC_CONNECTOR",
      "required": false,
      "note": "Only when more than one connector is installed: `@misiki/cs-cart-connector` names the one this build runs on."
    }
  ],
  "envNote": "`init.ts` hands the connector every `PUBLIC_CS_CART_*` variable that is set, camel-cased. The guide also lists `PUBLIC_CS_CART_ACCESS_TOKEN` and `PUBLIC_CS_CART_ACCESS_KEY`; the connector’s source does not read them.",
  "backendSettings": [
    "Generate an API key for an admin in the CS-Cart admin panel — and read the limitations before putting it anywhere a browser can see.",
    "Let the storefront’s origin make cross-origin requests to CS-Cart (CORS): in production the browser calls `PUBLIC_CS_CART_API_URL` directly, not only the server."
  ],
  "howItWorks": [
    "CS-Cart has one REST surface under two version prefixes, authenticated with HTTP Basic: an admin’s e-mail as the username and their generated API key as the password. On CS-Cart Ultimate and Multi-Vendor many routes are company-scoped, and the connector reads the company ID from the store variable rather than assuming `1`.",
    "There is no checkout entity in the REST API: payment methods are configuration records, and the only call that places an order is `POST /api/orders`, which creates it unpaid. The connector puts that in `orderService.codCheckout()`, and leaves every capture unimplemented, because marking an order paid without verifying a payment provider’s signature would be a fraud hole.",
    "Installing the package is the whole switch. `vite.config.ts` resolves whichever `@misiki/*-connector` is installed, and `src/lib/core/connectors/init.ts` hands it every `PUBLIC_CS_CART_*` variable that is set, camel-cased, at boot — in the server hook for server rendering and in the client hook for the browser, which must reach the same URL in production.",
    "There is no Litekart API behind the storefront here, so store identity — name, logo, currency, menus, plugin toggles, theme colours — comes from `src/lib/core/connectors/default-store.json` merged under the default export of `kitcommerce.config.ts`. Until you override it, the store is called “Test”. Any Litekart REST path the connector still inherits is answered from that local data, or resolved empty, rather than requested."
  ],
  "limitations": [
    "The storefront cannot complete an order. Its payment step calls one method per gateway, and on CS-Cart each throws `NotSupportedError`; the connector’s `orderService.codCheckout()`, which does create an order, is not called by the storefront yet.",
    "Sign-in (auth): throws `NotSupportedError` — CS-Cart has no core endpoint that returns a shopper session, and a stub that signed anyone in for any password would be an auth bypass, not a placeholder.",
    "Saved addresses: not wired.",
    "Countries: not wired.",
    "States and regions: not wired.",
    "Currencies: not wired.",
    "Coupons: not wired.",
    "Wishlist: not wired.",
    "Pages that depend on a service that is not wired render their empty state rather than failing.",
    "The connector signs every call with an admin’s e-mail and API key, taken from `PUBLIC_CS_CART_API_KEY` and `PUBLIC_CS_CART_API_SECRET`. SvelteKit sends `PUBLIC_` variables to the browser, so every visitor to the store can read both and use that admin’s API access.",
    "Homepage sections other than the live product list come from the active theme’s static content — the standing rule for themes.",
    "The Conversational Shopping assistant is Litekart-only; its widget stays hidden when another connector is active."
  ],
  "troubleshooting": [
    {
      "problem": "Boot fails with “the cs-cart connector is active but PUBLIC_CS_CART_API_URL is not set”",
      "fix": "Add `PUBLIC_CS_CART_API_URL` to `.env`. A production Node build and Docker read `PUBLIC_*` from the process environment rather than `.env`, so set it on your deploy platform too."
    },
    {
      "problem": "The build stops with “Several commerce connectors are installed … Set PUBLIC_CONNECTOR to pick one”",
      "fix": "More than one `@misiki/*-connector` is in `package.json` and none is Litekart’s. Uninstall the ones you do not run — a fresh clone also lists `@misiki/vendure-connector` — or set `PUBLIC_CONNECTOR='@misiki/cs-cart-connector'`."
    },
    {
      "problem": "Boot fails with “PUBLIC_…_API_URL is set, but it configures a different backend than the one this build runs on”",
      "fix": "Another backend’s variable is still set, usually left over from the one you switched from. Remove it, or install that backend’s connector. `PUBLIC_LITEKART_API_URL` is exempt: it only points the dev proxy."
    },
    {
      "problem": "The store is called “Test”, or shows the wrong name and logo",
      "fix": "Store identity is static on this backend. Set `name`, `logo`, `currencyCode` and the rest in the default export of `kitcommerce.config.ts`; `src/lib/core/connectors/default-store.json` lists every field."
    },
    {
      "problem": "`[cs-cart] no native implementation for get /api/...` in the console",
      "fix": "The connector’s REST guard caught a path inherited from Litekart and answered it empty instead of requesting it. Each path is reported once. Harmless if CS-Cart has no such feature; if it has one, the fix belongs in the connector’s matching service."
    },
    {
      "problem": "`http proxy error: api/... ECONNREFUSED` in dev",
      "fix": "A Litekart REST path was requested from outside the connector, so its guard never saw it. Vite proxies `/api` to `PUBLIC_LITEKART_API_URL`, or to `localhost:7000`."
    },
    {
      "problem": "The build fails on `@misiki/litekart-connector`",
      "fix": "`@misiki/kitcommerce-core` declares it as a peer, and `vite.config.ts` redirects that specifier to whichever connector is installed. Make sure exactly one `@misiki/*-connector` is listed in `package.json`."
    }
  ],
  "faq": [
    {
      "q": "Can I run a CS-Cart store on Svelte Commerce today?",
      "a": "No. Shoppers can browse and fill a cart, but there is no shopper sign-in and the storefront cannot complete an order. The connector also authenticates with an admin’s credentials from `PUBLIC_` variables, which every visitor’s browser receives."
    },
    {
      "q": "How do I switch Svelte Commerce to CS-Cart?",
      "a": "Remove the connectors the storefront ships with, install `@misiki/cs-cart-connector`, set `PUBLIC_CS_CART_API_URL` in `.env` and start the dev server. `vite.config.ts` resolves whichever connector is installed, so no file in the storefront changes."
    },
    {
      "q": "Why can’t Svelte Commerce complete an order on CS-Cart?",
      "a": "CS-Cart’s REST API has no checkout resource. The only call that places an order, `POST /api/orders`, lives in the connector’s `orderService.codCheckout()`, and the storefront’s payment step calls per-gateway methods instead, which throw on CS-Cart."
    },
    {
      "q": "Has the CS-Cart connector been run against a real CS-Cart store?",
      "a": "Not against a live production instance — no Svelte Commerce connector has been. It was written by reading CS-Cart’s authoritative API source, and the Svelte Commerce maintainers ask CS-Cart’s own maintainers for a sandbox to run it against."
    }
  ],
  "docUrl": "https://github.com/itswadesh/svelte-commerce/blob/main/docs/CS-CART.md",
  "read": "2026-09-25"
}
