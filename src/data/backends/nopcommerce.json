{
  "slug": "nopcommerce",
  "name": "nopCommerce",
  "title": "nopCommerce SvelteKit Storefront — Svelte Commerce Connector",
  "description": "Svelte Commerce on nopCommerce via the Web API Frontend plugin: 24 of 43 services wired, but the storefront cannot place an order yet. Setup and limits.",
  "lede": "nopCommerce core ships no REST API, so this connector targets the Web API Frontend plugin’s contract, v4.50.09 from 2022. Browsing, cart and sign-in are wired; the storefront cannot place an order on it yet.",
  "verdict": "Browsing, cart and sign-in are wired, but the storefront cannot complete an order yet. nopCommerce checks out in five steps through its plugin, with payment chosen by a payment plugin’s system name. The connector wires those steps as nopCommerce-native methods, but the storefront’s payment step calls one method per gateway, and each of those throws `NotSupportedError`.",
  "quickStart": [
    {
      "text": "get the storefront",
      "cmd": "git clone https://github.com/itswadesh/svelte-commerce\ncd svelte-commerce"
    },
    {
      "text": "one connector at a time: remove the two the storefront ships with, add this one",
      "cmd": "npm uninstall @misiki/litekart-connector @misiki/vendure-connector\nnpm i @misiki/nopcommerce-connector"
    },
    {
      "text": "point it at your nopCommerce store",
      "cmd": "echo PUBLIC_NOPCOMMERCE_API_URL=https://your-store.example.com >> .env"
    },
    {
      "text": "run it",
      "cmd": "npm run dev"
    }
  ],
  "quickStartNote": "The guide removes only `@misiki/litekart-connector`. A fresh clone’s `package.json` also lists `@misiki/vendure-connector`, and with two connectors installed and neither of them Litekart’s, the build stops and asks for `PUBLIC_CONNECTOR` — so this removes both.",
  "env": [
    {
      "name": "PUBLIC_NOPCOMMERCE_API_URL",
      "required": true,
      "note": "Base URL of the nopCommerce store running the Web API Frontend plugin. Boot fails, naming this variable, if it is missing."
    },
    {
      "name": "PUBLIC_NOPCOMMERCE_ACCESS_TOKEN",
      "required": false,
      "note": "A customer token to start from. Without one, the connector requests a guest token before the first cart or wishlist call."
    },
    {
      "name": "PUBLIC_CONNECTOR",
      "required": false,
      "note": "Only when more than one connector is installed: `@misiki/nopcommerce-connector` names the one this build runs on."
    }
  ],
  "envNote": "`init.ts` hands the connector every `PUBLIC_NOPCOMMERCE_*` variable that is set, camel-cased. The guide also lists `PUBLIC_NOPCOMMERCE_API_KEY`, `PUBLIC_NOPCOMMERCE_API_SECRET`, `PUBLIC_NOPCOMMERCE_ACCESS_KEY`, `PUBLIC_NOPCOMMERCE_STORE_ID` and `PUBLIC_NOPCOMMERCE_CHANNEL_ID`; the connector’s source does not read them.",
  "backendSettings": [
    "Install nopCommerce’s Web API Framework and Web API Frontend plugins. Core ships no REST API; the connector targets the Frontend plugin’s v4.50.09 contract.",
    "Let the storefront’s origin make cross-origin requests to nopCommerce (CORS): in production the browser calls `PUBLIC_NOPCOMMERCE_API_URL` directly, not only the server."
  ],
  "howItWorks": [
    "The connector speaks the Web API Frontend plugin’s contract (v4.50.09, 2022), not nopCommerce core, which has no REST API. A shopper who has not signed in gets a guest token before the first cart call, and tokens travel as `Authorization: Bearer`.",
    "Checkout in nopCommerce is five steps — billing address, shipping method, payment method, payment info, confirm — with the payment chosen by a payment plugin’s system name, and redirect gateways finishing on the store’s own return URL. The connector wires those steps as nopCommerce-native methods; the storefront’s per-gateway methods throw `NotSupportedError`, because an empty answer from a checkout call reads as “payment started”.",
    "Installing the package is the whole switch. `vite.config.ts` resolves whichever `@misiki/*-connector` is installed, and `src/lib/core/connectors/init.ts` hands it every `PUBLIC_NOPCOMMERCE_*` variable that is set, camel-cased, at boot — in the server hook for server rendering and in the client hook for the browser, which must reach the same URL in production.",
    "There is no Litekart API behind the storefront here, so store identity — name, logo, currency, menus, plugin toggles, theme colours — comes from `src/lib/core/connectors/default-store.json` merged under the default export of `kitcommerce.config.ts`. Until you override it, the store is called “Test”. Any Litekart REST path the connector still inherits is answered from that local data, or resolved empty, rather than requested."
  ],
  "limitations": [
    "The storefront cannot place an order. The storefront’s payment step calls one method per gateway — cash on delivery, Razorpay, Stripe and the rest — and nopCommerce has no per-gateway route, so each throws `NotSupportedError`. The connector’s nopCommerce-native checkout steps are not called by the storefront yet.",
    "Coupons: not wired.",
    "Currencies: not wired.",
    "Pages that depend on a service that is not wired render their empty state rather than failing.",
    "Homepage sections other than the live product list come from the active theme’s static content — the standing rule for themes.",
    "The Conversational Shopping assistant is Litekart-only; its widget stays hidden when another connector is active."
  ],
  "troubleshooting": [
    {
      "problem": "Every authenticated call returns 401",
      "fix": "The connector sends `Authorization: Bearer <token>`, as the plugin’s security scheme describes; the plugin’s own curl example sends the bare token. If your store expects the bare token, set `BaseService.AUTH_RAW_TOKEN = true` once at startup."
    },
    {
      "problem": "Boot fails with “the nopcommerce connector is active but PUBLIC_NOPCOMMERCE_API_URL is not set”",
      "fix": "Add `PUBLIC_NOPCOMMERCE_API_URL` to `.env`. A production Node build and Docker read `PUBLIC_*` from the process environment rather than `.env`, so set it on your deploy platform too."
    },
    {
      "problem": "The build stops with “Several commerce connectors are installed … Set PUBLIC_CONNECTOR to pick one”",
      "fix": "More than one `@misiki/*-connector` is in `package.json` and none is Litekart’s. Uninstall the ones you do not run — a fresh clone also lists `@misiki/vendure-connector` — or set `PUBLIC_CONNECTOR='@misiki/nopcommerce-connector'`."
    },
    {
      "problem": "Boot fails with “PUBLIC_…_API_URL is set, but it configures a different backend than the one this build runs on”",
      "fix": "Another backend’s variable is still set, usually left over from the one you switched from. Remove it, or install that backend’s connector. `PUBLIC_LITEKART_API_URL` is exempt: it only points the dev proxy."
    },
    {
      "problem": "The store is called “Test”, or shows the wrong name and logo",
      "fix": "Store identity is static on this backend. Set `name`, `logo`, `currencyCode` and the rest in the default export of `kitcommerce.config.ts`; `src/lib/core/connectors/default-store.json` lists every field."
    },
    {
      "problem": "`[nopcommerce] no native implementation for get /api/...` in the console",
      "fix": "The connector’s REST guard caught a path inherited from Litekart and answered it empty instead of requesting it. Each path is reported once. Harmless if nopCommerce has no such feature; if it has one, the fix belongs in the connector’s matching service."
    },
    {
      "problem": "`http proxy error: api/... ECONNREFUSED` in dev",
      "fix": "A Litekart REST path was requested from outside the connector, so its guard never saw it. Vite proxies `/api` to `PUBLIC_LITEKART_API_URL`, or to `localhost:7000`."
    },
    {
      "problem": "The build fails on `@misiki/litekart-connector`",
      "fix": "`@misiki/kitcommerce-core` declares it as a peer, and `vite.config.ts` redirects that specifier to whichever connector is installed. Make sure exactly one `@misiki/*-connector` is listed in `package.json`."
    }
  ],
  "faq": [
    {
      "q": "Can I run a nopCommerce store on Svelte Commerce today?",
      "a": "Not to take orders. Browsing, cart and sign-in are wired through the Web API Frontend plugin, but the storefront’s payment step calls per-gateway methods that throw on nopCommerce. The connector wires nopCommerce’s own five-step checkout; the storefront does not call it yet."
    },
    {
      "q": "How do I switch Svelte Commerce to nopCommerce?",
      "a": "Remove the connectors the storefront ships with, install `@misiki/nopcommerce-connector`, set `PUBLIC_NOPCOMMERCE_API_URL` in `.env` and start the dev server. `vite.config.ts` resolves whichever connector is installed, so no file in the storefront changes."
    },
    {
      "q": "Does nopCommerce need a plugin for Svelte Commerce?",
      "a": "Yes. nopCommerce core ships no REST API, so the connector targets the Web API Frontend plugin’s contract, version 4.50.09 from 2022, which needs the Web API Framework plugin too. If a newer contract is published, the Svelte Commerce maintainers have asked to be pointed at it."
    },
    {
      "q": "Has the nopCommerce connector been run against a real nopCommerce store?",
      "a": "Not against a live production instance — no Svelte Commerce connector has been. It was written by reading nopCommerce’s authoritative API source, and the Svelte Commerce maintainers ask nopCommerce’s own maintainers for a sandbox to run it against."
    }
  ],
  "openQuestion": "If there is a published API contract newer than Web API Frontend v4.50.09, where is it?",
  "docUrl": "https://github.com/itswadesh/svelte-commerce/blob/main/docs/NOPCOMMERCE.md",
  "read": "2026-09-25"
}
