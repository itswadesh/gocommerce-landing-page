{
  "slug": "commercetools",
  "name": "commercetools",
  "title": "commercetools SvelteKit Storefront — Svelte Commerce",
  "description": "Svelte Commerce on commercetools: 21 of 43 services wired, with cart, checkout and sign-in through the /me endpoints. Setup, environment, limits and fixes.",
  "lede": "The commercetools connector models version-based optimistic concurrency — `{ version, actions: [] }` — throughout, and is wired against the `/me` endpoints so nothing runs unscoped. Cart, checkout and sign-in are wired.",
  "verdict": "Cart, checkout and sign-in are wired, all through the `/me` endpoints; countries, currencies, CMS pages and vendors are not.",
  "quickStart": [
    {
      "text": "get the storefront",
      "cmd": "git clone https://github.com/itswadesh/svelte-commerce\ncd svelte-commerce"
    },
    {
      "text": "one connector at a time: remove the two the storefront ships with, add this one",
      "cmd": "npm uninstall @misiki/litekart-connector @misiki/vendure-connector\nnpm i @misiki/commercetools-connector"
    },
    {
      "text": "point it at your commercetools project",
      "cmd": "echo PUBLIC_COMMERCETOOLS_API_URL=https://api.europe-west1.gcp.commercetools.com/your-project-key >> .env\necho PUBLIC_COMMERCETOOLS_API_KEY=... >> .env\necho PUBLIC_COMMERCETOOLS_API_SECRET=... >> .env"
    },
    {
      "text": "run it",
      "cmd": "npm run dev"
    }
  ],
  "quickStartNote": "The guide removes only `@misiki/litekart-connector`. A fresh clone’s `package.json` also lists `@misiki/vendure-connector`, and with two connectors installed and neither of them Litekart’s, the build stops and asks for `PUBLIC_CONNECTOR` — so this removes both.",
  "env": [
    {
      "name": "PUBLIC_COMMERCETOOLS_API_URL",
      "required": true,
      "note": "The project-scoped root, `https://api.{region}.commercetools.com/{projectKey}`. Boot fails, naming this variable, if it is missing."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_API_KEY",
      "required": false,
      "note": "The API client’s `client_id`, sent as HTTP Basic to the auth host only."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_API_SECRET",
      "required": false,
      "note": "The API client’s `client_secret`. See the limitations."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_ACCESS_TOKEN",
      "required": false,
      "note": "Sent as a bearer token when set."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_STORE_ID",
      "required": false,
      "note": "A Store key: requests go through `/in-store/key={storeKey}`."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_CHANNEL_ID",
      "required": false,
      "note": "A Channel ID, sent as a reference in request bodies and as the price channel on reads — not as a path segment."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_REGION",
      "required": false,
      "note": "Such as `europe-west1.gcp`. Needed only when it cannot be read from the API URL."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_PROJECT_KEY",
      "required": false,
      "note": "Defaults to the last path segment of the API URL."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_AUTH_URL",
      "required": false,
      "note": "OAuth host override. Defaults to the API host with `api.` replaced by `auth.`."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_SCOPE",
      "required": false,
      "note": "Space-separated OAuth scopes requested when a token is issued."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_LOCALE",
      "required": false,
      "note": "Used to flatten localised strings and build `where` and `sort` predicates. Defaults to `en`."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_CURRENCY",
      "required": false,
      "note": "ISO 4217 currency for cart and price selection."
    },
    {
      "name": "PUBLIC_COMMERCETOOLS_COUNTRY",
      "required": false,
      "note": "Country code for price selection."
    },
    {
      "name": "PUBLIC_CONNECTOR",
      "required": false,
      "note": "Only when more than one connector is installed: `@misiki/commercetools-connector` names the one this build runs on."
    }
  ],
  "envNote": "`init.ts` hands the connector every `PUBLIC_COMMERCETOOLS_*` variable that is set, camel-cased: `PUBLIC_COMMERCETOOLS_PROJECT_KEY` arrives as `projectKey`. The guide also lists `PUBLIC_COMMERCETOOLS_ACCESS_KEY`; the connector’s source does not read it.",
  "backendSettings": [
    "Create an API client for the storefront with only the scopes a shopper’s session needs: its ID and secret reach the browser.",
    "Let the storefront’s origin make cross-origin requests to commercetools (CORS): in production the browser calls `PUBLIC_COMMERCETOOLS_API_URL` directly, not only the server."
  ],
  "howItWorks": [
    "commercetools updates a resource by sending its current `version` and a list of `actions`; the connector models that throughout, so a stale write fails rather than overwrites. It works through the `/me` endpoints, scoped to the signed-in or anonymous shopper, so no call returns another customer’s data. Tokens come from the auth host, with the API client’s ID and secret as HTTP Basic.",
    "Installing the package is the whole switch. `vite.config.ts` resolves whichever `@misiki/*-connector` is installed, and `src/lib/core/connectors/init.ts` hands it every `PUBLIC_COMMERCETOOLS_*` variable that is set, camel-cased, at boot — in the server hook for server rendering and in the client hook for the browser, which must reach the same URL in production.",
    "There is no Litekart API behind the storefront here, so store identity — name, logo, currency, menus, plugin toggles, theme colours — comes from `src/lib/core/connectors/default-store.json` merged under the default export of `kitcommerce.config.ts`. Until you override it, the store is called “Test”. Any Litekart REST path the connector still inherits is answered from that local data, or resolved empty, rather than requested."
  ],
  "limitations": [
    "Countries: not wired.",
    "Currencies: not wired.",
    "CMS pages: not wired — legal and CMS pages render their layout with empty content.",
    "Vendors: not wired.",
    "Pages that depend on a service that is not wired render their empty state rather than failing.",
    "The API client’s ID and secret come from `PUBLIC_COMMERCETOOLS_API_KEY` and `PUBLIC_COMMERCETOOLS_API_SECRET`. SvelteKit sends `PUBLIC_` variables to the browser, so every visitor can read them; the client’s scopes are what limit the damage.",
    "Homepage sections other than the live product list come from the active theme’s static content — the standing rule for themes.",
    "The Conversational Shopping assistant is Litekart-only; its widget stays hidden when another connector is active."
  ],
  "troubleshooting": [
    {
      "problem": "Boot fails with “the commercetools connector is active but PUBLIC_COMMERCETOOLS_API_URL is not set”",
      "fix": "Add `PUBLIC_COMMERCETOOLS_API_URL` to `.env`. A production Node build and Docker read `PUBLIC_*` from the process environment rather than `.env`, so set it on your deploy platform too."
    },
    {
      "problem": "The build stops with “Several commerce connectors are installed … Set PUBLIC_CONNECTOR to pick one”",
      "fix": "More than one `@misiki/*-connector` is in `package.json` and none is Litekart’s. Uninstall the ones you do not run — a fresh clone also lists `@misiki/vendure-connector` — or set `PUBLIC_CONNECTOR='@misiki/commercetools-connector'`."
    },
    {
      "problem": "Boot fails with “PUBLIC_…_API_URL is set, but it configures a different backend than the one this build runs on”",
      "fix": "Another backend’s variable is still set, usually left over from the one you switched from. Remove it, or install that backend’s connector. `PUBLIC_LITEKART_API_URL` is exempt: it only points the dev proxy."
    },
    {
      "problem": "The store is called “Test”, or shows the wrong name and logo",
      "fix": "Store identity is static on this backend. Set `name`, `logo`, `currencyCode` and the rest in the default export of `kitcommerce.config.ts`; `src/lib/core/connectors/default-store.json` lists every field."
    },
    {
      "problem": "`[commercetools] no native implementation for get /api/...` in the console",
      "fix": "The connector’s REST guard caught a path inherited from Litekart and answered it empty instead of requesting it. Each path is reported once. Harmless if commercetools has no such feature; if it has one, the fix belongs in the connector’s matching service."
    },
    {
      "problem": "`http proxy error: api/... ECONNREFUSED` in dev",
      "fix": "A Litekart REST path was requested from outside the connector, so its guard never saw it. Vite proxies `/api` to `PUBLIC_LITEKART_API_URL`, or to `localhost:7000`."
    },
    {
      "problem": "The build fails on `@misiki/litekart-connector`",
      "fix": "`@misiki/kitcommerce-core` declares it as a peer, and `vite.config.ts` redirects that specifier to whichever connector is installed. Make sure exactly one `@misiki/*-connector` is listed in `package.json`."
    }
  ],
  "faq": [
    {
      "q": "Can I run a commercetools project on Svelte Commerce today?",
      "a": "Cart, checkout and sign-in are wired through the `/me` endpoints; countries, currencies, CMS pages and vendors are not. Like every connector, it has not been run against a live production commercetools project."
    },
    {
      "q": "How do I switch Svelte Commerce to commercetools?",
      "a": "Remove the connectors the storefront ships with, install `@misiki/commercetools-connector`, set `PUBLIC_COMMERCETOOLS_API_URL` in `.env` and start the dev server. `vite.config.ts` resolves whichever connector is installed, so no file in the storefront changes."
    },
    {
      "q": "Does the connector use the /me endpoints or the Checkout API?",
      "a": "The `/me` endpoints, with an anonymous token for guests, so nothing runs unscoped. The Svelte Commerce maintainers have asked commercetools whether that is the idiomatic pattern for a headless storefront, or whether the Checkout and Frontend APIs are."
    },
    {
      "q": "Has the commercetools connector been run against a real commercetools store?",
      "a": "Not against a live production instance — no Svelte Commerce connector has been. It was written by reading commercetools’s authoritative API source, and the Svelte Commerce maintainers ask commercetools’s own maintainers for a sandbox to run it against."
    }
  ],
  "openQuestion": "For a headless storefront, is the `/me` surface plus an anonymous token the idiomatic pattern, or should the connector use the Checkout or Frontend APIs instead?",
  "docUrl": "https://github.com/itswadesh/svelte-commerce/blob/main/docs/COMMERCETOOLS.md",
  "read": "2026-09-25"
}
